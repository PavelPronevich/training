@model IEnumerable<HandH.Models.Manager>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @if (User.IsInRole("admin")){@Html.ActionLink("Create New", "Create")}
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ManagerSurname)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ManagerSurname)
        </td>
        <td>
            @if (User.IsInRole("admin")){@Html.ActionLink("Edit", "Edit", new { id=item.ManagerID })}
            @Html.ActionLink("Details", "Details", new { id=item.ManagerID }) 
            @if (User.IsInRole("admin")){@Html.ActionLink("Delete", "Delete", new { id=item.ManagerID })}
        </td>
    </tr>
}

</table>
