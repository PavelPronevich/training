@model IEnumerable<HandH.Models.Customer>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @if (User.IsInRole("admin")){@Html.ActionLink("Create New", "Create")}
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CustomerName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CustomerName)
        </td>
        <td>
            @if (User.IsInRole("admin")){@Html.ActionLink("Edit", "Edit", new { id=item.CustomerID })}
            @Html.ActionLink("Details", "Details", new { id=item.CustomerID })
            @if (User.IsInRole("admin")){@Html.ActionLink("Delete", "Delete", new { id=item.CustomerID })}
        </td>
    </tr>
}

</table>
